# 2026-07-16（Day 8）开发日志

人员：C1、C2

## C1 完成内容

### 1. 重试协调——参数保留

- `TaskSnapshot` 新增 `generation_params: dict` 字段，`create()` 时存入全部生成参数（instruction/attachments/num_pages/powerpoint_type/template/convert_type/enable_planner/language）
- `retry()` 不再硬编码 `attachments=[]`、`template=None`，改为从 `snapshot.generation_params` 恢复原始参数
- 重试时清空 `error_message`

### 2. 导出协调——文件存在性检查

- `export()` 增强：先检查 `result_artifact` 是否在磁盘上存在
- 不存在时扫描 `exports/` 目录，按修改时间排序取最新的 `.pptx`
- 仍找不到返回 None（前端收到 409）

### 3. 并发控制

- `TaskManager` 新增 `max_concurrent` 参数（默认 2），构造 `asyncio.Semaphore`
- `create()` 和 `retry()` 通过 `_runner_with_semaphore()` 包装，信号量耗尽时后续任务排队等待
- `app.py` 通过 `DEEPPRESENTER_MAX_CONCURRENT_TASKS` 环境变量配置，默认 2

### 4. 事件异常处理加固

- `EventReporter.emit()` 增加 try/except，发布失败只发 `RuntimeWarning`，不中断生成流程
- 避免 EventBus 关闭后或磁盘满时导致任务崩溃

### 5. Python 3.9 兼容修复

- C2 的 `preview.py` f-string 内含 `\"` 转义（3.12 特性），改为拼接字符串

### 6. 单元测试

新增 7 个测试用例：

- **参数保留**（3 个）：快照存储参数、retry 恢复参数、retry 清空错误信息
- **导出兜底**（2 个）：artifact 不在磁盘时扫描 fallback、无产物返回 None
- **并发限制**（2 个）：max_concurrent=1 排队正确、max_concurrent 配置生效

## 改动文件

```
deeppresenter/server/services/task_manager.py  ← generation_params + Semaphore + export 增强
deeppresenter/server/services/event_reporter.py ← emit() try/except
deeppresenter/server/services/preview.py        ← f-string 3.9 兼容
deeppresenter/server/app.py                     ← max_concurrent 配置
deeppresenter/server/tests/test_task_manager.py ← +7 个测试
```

## 测试结果

34 个 TaskManager 测试全部通过；全量 92/97 通过（5 个失败为 C2 Windows 路径分隔符存量问题）。

## C2 完成内容

### 1. 合并后测试修复

- 拉取远端 `c3d7b51` 后复现全量测试：98/101 通过，2 个失败为 retry 参数测试误触真实 AgentLoop/Docker。
- 新增 `RecordingRetryManager` 测试替身，retry 参数测试改为纯单元测试，不再依赖 Docker、LLM 或 MCP。
- `TaskManager.create()` / `retry()` 调整为获得 semaphore 后再创建 runner coroutine，避免排队任务取消或测试退出时出现 coroutine never awaited。

### 2. 异常场景补测

- EventReporter 发布失败：确认只发 `RuntimeWarning`，不向上抛异常。
- Preview renderer 失败：确认不写 `current.json` 和 `slides/index.json`，避免半成品污染当前页。
- slides index 损坏：确认 `list_slides()` 能回退扫描 `current.json`。
- artifact API：补路径穿越 403、缺失产物 404。
- slides API：补未知 slide 404。
- create task：补缺少 instruction 时 422。

### 3. 测试结果

- retry 相关测试：`6 passed`
- C2 预览/路由/事件异常测试：`27 passed, 1 skipped`
- server 全量测试：`107 passed, 1 skipped in 3.69s`
- 真实 AgentLoop 端到端复测：任务 `3ae618ee` 成功完成，事件包含 `slide.preview_ready`、`export.completed`、`task.completed`，产物为 `.local-workspace/3ae618ee/项目收尾测试.pptx`

### 4. 前端工作台联调

- 使用 `feat/frontend-workbench` 临时 worktree 验证前端工作台，不直接 merge 该分支。
- 补齐临时缺失 UI 组件和后端 API 适配后，前端 `npm run build` 通过。
- 当前 C2 后端跑在 `127.0.0.1:8001`，Vite 通过 `/api` 代理联调。
- 真实任务 `c3b69265` 成功生成，网页显示“生成完成 · 共 1 页”，左侧缩略图和主画布均加载 `preview.png`，浏览器识别尺寸为 `1280x720`。
- 发现前端终态后仍显示“连接中断，正在重连”，临时修为仅 running 状态展示。

### 5. 预览和导出收尾

- 多页真实任务 `6ee93905` 复测通过：两页均按 `slide.started -> slide.preview_ready -> slide.completed` 发事件，前端不再停留在“排队中”。
- 预览渲染倍率调整为 1.5，真实 PNG 预览尺寸为 `1920x1080`。
- 修复 PDF 导出：`POST /api/tasks/{task_id}/export` 支持 `format=pdf|pptx`，PDF 请求返回真实 `.pdf` 产物和 `filename`，避免前端将 PPTX 字节保存为 `.pdf` 导致无法打开。

## C2 改动文件

```
deeppresenter/server/services/task_manager.py      ← 延迟创建 runner coroutine
deeppresenter/server/tests/test_task_manager.py    ← retry 测试替身
deeppresenter/server/tests/test_event_reporter.py  ← 事件发布失败异常测试
deeppresenter/server/tests/test_preview.py         ← 预览失败/index 损坏异常测试
deeppresenter/server/tests/test_routes_slides.py   ← artifact/slides/create 异常路由测试
docs/c2-progress.md                                ← C2 进度同步
docs/devlog/0716.md                                ← 当天 devlog 补充
PPTAgent-frontend-workbench/frontend/src/*         ← 临时前端联调补丁，不属于当前后端分支提交范围
```
